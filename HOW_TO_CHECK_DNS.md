# Как проверить DNS для marketingbot.trycloudflare.com

## 🔍 Способы проверки DNS

### Способ 1: Через командную строку (Mac/Linux)

Откройте терминал и выполните:

```bash
nslookup marketingbot.trycloudflare.com 8.8.8.8
```

**Если DNS работает**, вы увидите:
```
Server:		8.8.8.8
Address:	8.8.8.8#53

Non-authoritative answer:
Name:	marketingbot.trycloudflare.com
Address: 198.41.200.XX  (или другой IP адрес Cloudflare)
```

**Если DNS не работает**, вы увидите:
```
** server can't find marketingbot.trycloudflare.com: NXDOMAIN
```

### Способ 2: Через команду dig (Mac/Linux)

```bash
dig marketingbot.trycloudflare.com @8.8.8.8
```

**Если DNS работает**, вы увидите IP адрес в секции "ANSWER SECTION":
```
;; ANSWER SECTION:
marketingbot.trycloudflare.com. 300 IN A 198.41.200.XX
```

**Если DNS не работает**, секция "ANSWER SECTION" будет пустой.

### Способ 3: Через браузер

Откройте в браузере:
```
https://marketingbot.trycloudflare.com/api/promotions
```

**Если DNS работает:**
- Страница загрузится и покажет JSON с акциями
- Или покажет страницу Cloudflare (но не ошибку "Could not resolve host")

**Если DNS не работает:**
- Браузер покажет ошибку "Не удается найти сайт" или "DNS_PROBE_FINISHED_NXDOMAIN"

### Способ 4: Через curl (Mac/Linux)

```bash
curl -v https://marketingbot.trycloudflare.com/api/promotions
```

**Если DNS работает:**
- Вы увидите HTTP ответ с данными акций
- Или HTTP ответ от Cloudflare

**Если DNS не работает:**
- Вы увидите ошибку: `Could not resolve host: marketingbot.trycloudflare.com`

### Способ 5: Онлайн сервисы

Можно использовать онлайн DNS проверки:

1. **https://dnschecker.org/**
   - Введите: `marketingbot.trycloudflare.com`
   - Выберите тип записи: `A`
   - Нажмите "Search"
   - Если DNS работает, увидите IP адреса в разных локациях

2. **https://www.whatsmydns.net/**
   - Введите домен: `marketingbot.trycloudflare.com`
   - Выберите тип: `A Record`
   - Нажмите "Search"
   - Если DNS работает, увидите IP адреса

3. **https://mxtoolbox.com/DNSLookup.aspx**
   - Введите домен: `marketingbot.trycloudflare.com`
   - Нажмите "DNS Lookup"
   - Если DNS работает, увидите A записи с IP адресами

## ✅ Что делать после проверки

### Если DNS разрешается:

1. Проверьте API:
   ```bash
   curl https://marketingbot.trycloudflare.com/api/promotions
   ```
   Должен вернуть JSON с акциями.

2. Откройте мини-приложение в Telegram:
   - Бот → Личный кабинет → Акции
   - Акции должны загрузиться автоматически

3. Проверьте консоль браузера (F12):
   - Не должно быть ошибок "Failed to fetch" или "Could not resolve host"
   - Должны быть логи: "✅ Акции загружены успешно"

### Если DNS все еще не разрешается:

1. Подождите еще 30-60 минут
2. Или пересоздайте маршрут в Cloudflare Dashboard:
   - Удалите маршрут `marketingbot.trycloudflare.com`
   - Создайте его заново
   - Подождите 10-15 минут

## 🔄 Автоматическая проверка

Можно создать скрипт для автоматической проверки:

```bash
#!/bin/bash
# check_dns.sh

echo "Проверка DNS для marketingbot.trycloudflare.com..."
echo ""

# Проверка через nslookup
if nslookup marketingbot.trycloudflare.com 8.8.8.8 2>&1 | grep -q "Address:"; then
    echo "✅ DNS разрешается!"
    echo ""
    echo "Проверка API..."
    if curl -s https://marketingbot.trycloudflare.com/api/promotions | grep -q "title"; then
        echo "✅ API работает!"
    else
        echo "⚠️  API не отвечает"
    fi
else
    echo "❌ DNS не разрешается. Подождите еще."
fi
```

Сохраните как `check_dns.sh`, сделайте исполняемым (`chmod +x check_dns.sh`) и запускайте:
```bash
./check_dns.sh
```

## 📱 Быстрая проверка прямо сейчас

Выполните эту команду в терминале:

```bash
nslookup marketingbot.trycloudflare.com 8.8.8.8
```

Или откройте в браузере:
```
https://marketingbot.trycloudflare.com/api/promotions
```

Если видите данные или IP адрес - DNS работает! 🎉
