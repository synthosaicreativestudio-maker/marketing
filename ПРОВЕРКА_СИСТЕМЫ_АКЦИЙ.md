# 🔍 Проверка системы загрузки акций

## ✅ Что уже проверено и исправлено

### 1. Код загрузки акций (menu.html)
- ✅ URL Google Apps Script обновлен на актуальный
- ✅ Добавлена детальная обработка ошибок
- ✅ Добавлено логирование всех попыток загрузки
- ✅ Улучшена обработка HTML ответов (если GAS вернет HTML вместо JSON)
- ✅ Добавлена проверка Content-Type
- ✅ Правильная обработка редиректов
- ✅ Fallback на Cloudflare Tunnel и локальный API

### 2. Google Apps Script (GOOGLE_APPS_SCRIPT_FULL.js)
- ✅ Функция `doGet()` правильно настроена
- ✅ Возвращает JSON массив акций
- ✅ Фильтрует только активные акции (статус "Активна")
- ✅ Правильно обрабатывает ошибки

### 3. Git и развертывание
- ✅ Все изменения закоммичены
- ✅ Изменения отправлены в GitHub
- ✅ GitHub Pages автоматически обновится через 1-2 минуты

## 🔧 Что нужно проверить вручную

### 1. Google Apps Script развертывание

**Проверьте в Google Apps Script:**

1. Откройте [Google Apps Script](https://script.google.com)
2. Найдите ваш проект с акциями
3. Убедитесь, что функция `doGet()` есть в проекте
4. Нажмите "Развернуть" → "Новое развертывание"
5. Выберите тип: "Веб-приложение"
6. Настройки:
   - **Описание:** "API для акций"
   - **Выполнять от имени:** "Меня"
   - **У кого есть доступ:** "Все"
7. Нажмите "Развернуть"
8. **Скопируйте URL развертывания** - он должен быть вида:
   ```
   https://script.google.com/macros/s/AKfycbzQ8pDvdc6ZriqchBvto6prPk0OOwTsHiUx_GAZhkCtXQjtCfM5-KZO4uH7zIUCgbqOsA/exec
   ```
9. Убедитесь, что URL в `menu.html` совпадает с URL развертывания

### 2. Проверка URL в коде

**Текущий URL в menu.html:**
```
https://script.google.com/macros/s/AKfycbzQ8pDvdc6ZriqchBvto6prPk0OOwTsHiUx_GAZhkCtXQjtCfM5-KZO4uH7zIUCgbqOsA/exec
```

**Если URL отличается:**
1. Откройте `menu.html`
2. Найдите строку 775
3. Замените URL на актуальный из развертывания
4. Сохраните и закоммитьте изменения

### 3. Проверка Google Sheets

**Убедитесь, что:**
1. Таблица "Акции" существует
2. Есть активные акции со статусом "Активна"
3. Колонки правильно названы:
   - A: ID (или пусто)
   - B: Название
   - C: Описание
   - D: Статус (должно быть "Активна")
   - E: Дата начала
   - F: Дата окончания
   - G: Контент (опционально)

### 4. Тестирование Google Apps Script

**В редакторе Google Apps Script:**

1. Откройте функцию `testGetPromotions()` (строка 354)
2. Нажмите "Выполнить"
3. Проверьте логи (Вид → Логи)
4. Должен вернуться JSON массив с акциями

**Или протестируйте URL напрямую в браузере:**
```
https://script.google.com/macros/s/AKfycbzQ8pDvdc6ZriqchBvto6prPk0OOwTsHiUx_GAZhkCtXQjtCfM5-KZO4uH7zIUCgbqOsA/exec
```

Должен вернуться JSON, например:
```json
[
  {
    "id": "test_тест_функции_акции_09.10.2025_11.10.2025",
    "title": "тест",
    "description": "тест функции акции",
    "status": "Активна",
    "start_date": "09.10.2025",
    "end_date": "11.10.2025",
    "content": "data:image/jpeg;base64,..."
  }
]
```

## 🐛 Диагностика проблем

### Проблема: "Failed to fetch" или "ERR_NAME_NOT_RESOLVED"

**Причина:** Проблема с DNS или сетью

**Решение:**
1. Проверьте, что Google Apps Script URL правильный
2. Попробуйте открыть URL напрямую в браузере
3. Если URL не открывается, проверьте развертывание в Google Apps Script

### Проблема: "Received HTML instead of JSON"

**Причина:** Google Apps Script вернул HTML страницу вместо JSON

**Возможные причины:**
1. URL неверный или развертывание не настроено
2. Требуется авторизация (проверьте настройки доступа)
3. Ошибка в скрипте

**Решение:**
1. Проверьте настройки развертывания (должно быть "Все" имеют доступ)
2. Проверьте, что функция `doGet()` есть в скрипте
3. Проверьте логи в Google Apps Script (Вид → Логи)

### Проблема: "Invalid JSON response"

**Причина:** Google Apps Script вернул невалидный JSON

**Решение:**
1. Проверьте логи в Google Apps Script
2. Убедитесь, что функция `doGet()` правильно возвращает JSON
3. Проверьте, что в таблице есть данные

### Проблема: Пустой массив акций

**Причина:** Нет активных акций в таблице

**Решение:**
1. Проверьте Google Sheets
2. Убедитесь, что есть акции со статусом "Активна"
3. Проверьте, что колонка "Статус" правильно названа

## 📊 Логи в консоли браузера

После обновления страницы в консоли браузера (F12) вы увидите:

```
✅ Google Apps Script URL добавлен: https://script.google.com/...
📡 Попытка загрузить акции с 2 источников: ...
🔄 [1/2] Попытка загрузить акции с: Google Apps Script (...)
📊 Google Apps Script - Response status: 200, ok: true, type: cors
📥 Google Apps Script - Ответ получен (XXX символов)
✅ Google Apps Script - Данные получены: [...]
✅ ✅ ✅ Акции загружены успешно с: Google Apps Script (X акций)
```

Если есть ошибки, они будут детально описаны в консоли.

## ✅ Итоговая проверка

1. ✅ Код обновлен и отправлен в GitHub
2. ⏳ Дождитесь обновления GitHub Pages (1-2 минуты)
3. ⏳ Откройте мини-приложение в Telegram
4. ⏳ Откройте консоль браузера (F12)
5. ⏳ Проверьте логи загрузки акций

Если все правильно настроено, акции должны загрузиться с Google Apps Script.
