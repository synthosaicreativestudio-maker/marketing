# Инструкция по применению исправлений

## Что было исправлено

1. ✅ **webhook_handler.py** - заменены словари `web_app={'url': ...}` на `WebAppInfo(url=...)`
2. ✅ **promotions_notifier.py** - заменены словари `web_app={"url": ...}` на `WebAppInfo(url=...)`
3. ✅ **handlers/utils.py** - добавлена валидация URL для предотвращения подобных проблем

## Что нужно сделать

### 1. Закоммитить и запушить изменения

```bash
git add webhook_handler.py promotions_notifier.py handlers/utils.py FIX_WEBAPP_URL.md DEPLOY_FIXES.md update_yandex_server.sh
git commit -m "Fix: Исправлено использование WebAppInfo вместо словарей для мини-приложения"
git push origin main
```

### 2. Обновить код на сервере

**Вариант А: Использовать скрипт**
```bash
ssh ubuntu@158.160.0.127
cd /home/ubuntu/marketingbot
git pull origin main
bash update_yandex_server.sh
```

**Вариант Б: Вручную**
```bash
ssh ubuntu@158.160.0.127
cd /home/ubuntu/marketingbot
git pull origin main

# Проверить WEB_APP_URL
grep WEB_APP_URL .env

# Если нужно исправить:
sed -i 's|WEB_APP_URL=.*|WEB_APP_URL=https://synthosaicreativestudio-maker.github.io/marketing/|' .env

# Перезапустить сервисы
sudo systemctl restart marketingbot-bot.service
sudo systemctl restart marketingbot-web.service

# Проверить статус
sudo systemctl status marketingbot-bot.service
sudo systemctl status marketingbot-web.service
```

### 3. Проверить работу мини-приложения

1. Открыть бота в Telegram
2. Нажать кнопку "👤 Личный кабинет"
3. Проверить, что мини-приложение загружается без ошибок
4. Открыть консоль браузера (F12) и проверить отсутствие ошибок загрузки

### 4. Проверить логи на сервере

```bash
sudo journalctl -u marketingbot-bot.service -f
```

В логах не должно быть ошибок типа:
- "WEB_APP_URL содержит невалидный URL"
- Ошибок при создании кнопок WebApp

## Ожидаемый результат

После применения исправлений:
- ✅ Мини-приложение должно загружаться корректно
- ✅ URL должен быть правильным (без URL-encoded JSON данных)
- ✅ Кнопки WebApp должны работать корректно
- ✅ В логах не должно быть ошибок валидации URL
