# Правила работы с ботом MarketingBot

> [!IMPORTANT]
> **НЕУКОСНИТЕЛЬНОЕ СОБЛЮДЕНИЕ ЭТИХ ПРАВИЛ ОБЯЗАТЕЛЬНО ДЛЯ ВСЕХ РАЗРАБОТЧИКОВ.**
> Нарушение этих правил приводит к конфликтам версий и блокировке работы бота.

## 1. Защита от двойного запуска
Запуск двух экземпляров бота одновременно (например, на локальной машине и на сервере) **ЗАПРЕЩЕН**. Это приводит к ошибке `409 Conflict` и постоянным перезапускам.

**Правильный алгоритм действий при обновлении:**
1.  🛑 **ОСТАНОВИТЬ** бота на сервере (`sudo systemctl stop marketingbot` или Ctrl+C).
2.  📥 **ОБНОВИТЬ** код (`git pull`).
3.  ✅ **ЗАПУСТИТЬ** бота (`sudo systemctl start marketingbot` или `python3 bot.py`).

## 2. Тестирование
*   **Тестирование на сервере:** Основное тестирование должно проводиться на сервере, где окружение соответствует боевому.
*   **Тестирование локально:** Допускается ТОЛЬКО с использованием **отдельного тестового токена**. Запуск с боевым токеном на локальной машине при работающем сервере **ЗАПРЕЩЕН**.

## 3. Безопасность
*   ❌ **ЗАПРЕЩЕНО** коммитить API ключи, токены и пароли в Git (GitHub).
*   ВСЕ секретные данные должны находиться только в `.env` файле.
*   Перед коммитом проверьте `git status` и убедитесь, что `.env` не попадает в коммит.

## 4. Версионирование
*   В продакшн запускается только версия из ветки `main`.
*   Все изменения сначала пушатся в репозиторий, затем пуллятся на сервер. Не правьте файлы на сервере вручную!
