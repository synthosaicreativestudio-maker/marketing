# 🚀 Инструкция по обновлению и деплою проекта (MarketingBot)

Этот документ содержит проверенные данные для синхронизации кода и развертывания изменений.

---

## 🔐 1. Секреты и пути (Локально)

**НИКОГДА не коммитьте эти файлы в GitHub!**

| Файл | Описание | Путь (от корня проекта) | Переменная в .env |
| :--- | :--- | :--- | :--- |
| `.env` | Все токены и API ключи | `marketingbot/marketingbot/.env` | - |
| `credentials.json` | Google Service Account | `marketingbot/marketingbot/credentials.json` | `GCP_SA_FILE` |
| `ssh-key` | Приватный ключ Yandex | `ssh-key-1770366966512/ssh-key-1770366966512` | - |

---

## 📤 2. GitHub (Синхронизация)

**Репозиторий:** `https://github.com/synthosaicreativestudio-maker/marketing.git`

1.  **Зафиксировать изменения:** `git add .` && `git commit -m "Описание"`
2.  **Загрузить:** `git push origin main`

---

## 🛠️ 3. Деплой (Yandex Cloud)

**Параметры сервера Бота:**
- **IP:** `89.169.176.108`
- **Пользователь:** `marketing`
- **Директория:** `/home/marketing/marketingbot`

### Алгоритм обновления (вручную):
1.  **SSH:** `ssh -i <путь_к_ключу> marketing@89.169.176.108`
2.  **Обновление:**
    ```bash
    cd /home/marketing/marketingbot
    git pull origin main
    source .venv/bin/activate
    pip install -r requirements.txt  # если менялись зависимости
    ```
3.  **Перезапуск сервисов:**
    ```bash
    sudo systemctl restart marketingbot-bot.service  # Бот
    sudo systemctl restart marketingbot-web.service  # Webhook/API
    ```

---

## 🌐 4. US Прокси (Для Gemini)

Если бот не получает ответы от ИИ, проверьте прокси-сервер (США):
- **IP:** `37.1.212.51`
- **Порт:** `8443`
- **Конфигурация в .env:** `PROXYAPI_BASE_URL=http://37.1.212.51:8443`

---

## 📊 5. Диагностика
- **Мониторинг логов:** `journalctl -u marketingbot-bot.service -f`
- **Статус процессов:** `./scripts/check_bot_status.sh` (запускать локально)
- **Все доступы:** [ALL_CREDENTIALS.md](../ALL_CREDENTIALS.md)
