# 🎨 План Реализации: Premium Redesign

**Цель:** Создать "Вау-эффект" при открытии WebApp. Переход от утилитарного дизайна к премиальному (Glassmorphism).

## 1. Дизайн-концепция (Visual Style)
*   **Тема:** Dark Mode (Глубокий темный фон).
*   **Стиль:** Glassmorphism (Полупрозрачные карточки, размытие фона `backdrop-filter: blur`).
*   **Цвета:**
    *   Фон: Темно-синий/Черный градиент.
    *   Акцент: Неоновый фиолетовый (`#8A2BE2`) и голубой (`#00BFFF`).
    *   Текст: Белый (с разной прозрачностью для иерархии).
*   **Шрифты:** `Inter` или `Outfit` (Google Fonts).

## 2. Технические изменения (Frontend)

### 2.1. CSS Архитектура
*   Внедрение CSS Variables для цветов и отступов.
*   Сброс стандартных стилей браузера (Reset CSS).
*   Новые классы для эффекта стекла (`.glass-card`, `.glass-button`).

### 2.2. Компоненты
*   **Header:** Минималистичный, с приветствием и аватаром (заглушкой).
*   **Navigation:** Плавающее нижнее меню или стильные табы.
*   **Promotions Cards:**
    *   Вместо списка — красивые карточки с градиентной обводкой.
    *   Статус акции (Активна) в виде светящегося бейджа.
*   **Loading State:** Skeleton-анимация (мерцающие блоки) вместо спиннера.

### 2.3. Анимации
*   Плавное появление контента (`fade-in-up`).
*   Микро-анимации при клике на кнопки (`scale`).
*   Анимированный градиент на фоне.

## 3. План работ
1.  [ ] Подключить Google Fonts.
2.  [ ] Заменить CSS в `menu.html` на новые стили.
3.  [ ] Переверстать карточки акций.
4.  [ ] Добавить Skeleton Loader.
5.  [ ] Проверить адаптивность (Mobile First).

---
**Статус:** Подготовка макета.
