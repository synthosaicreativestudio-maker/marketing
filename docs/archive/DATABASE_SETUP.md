# Настройка работы с базой данных

## 🔄 Переключение между Google Sheets и БД

Бот поддерживает работу как с Google Sheets, так и с базой данных. Переключение происходит через переменную окружения.

### Использование базы данных (рекомендуется)

В файле `.env` добавьте:

```bash
USE_DATABASE=true
DATABASE_URL=sqlite:///./db/appeals.db
```

### Использование Google Sheets (старый способ)

В файле `.env` убедитесь, что:

```bash
USE_DATABASE=false
# или просто не указывайте USE_DATABASE
APPEALS_SHEET_ID=your_sheet_id
APPEALS_SHEET_NAME=обращения
GCP_SA_JSON=...
```

## 📦 Первоначальная настройка БД

### 1. Инициализация базы данных

```bash
python3.10 init_db.py
```

Это создаст файл `db/appeals.db` со всеми необходимыми таблицами.

### 2. Настройка переменных окружения

```bash
# В .env
USE_DATABASE=true
DATABASE_URL=sqlite:///./db/appeals.db
```

### 3. Запуск бота

```bash
python3.10 bot.py
```

Бот автоматически определит, что нужно использовать БД, и инициализирует её при первом запуске.

## 🔍 Проверка работы

### Проверить, что БД создана:

```bash
ls -la db/appeals.db
```

### Проверить содержимое БД:

```bash
sqlite3 db/appeals.db ".tables"
sqlite3 db/appeals.db "SELECT * FROM appeals LIMIT 5;"
```

## 📊 Структура БД

- `appeals` - обращения
- `appeal_messages` - сообщения в обращениях
- `specialist_responses` - ответы специалистов

## ⚠️ Важно

1. **Файл БД не коммитится в git** (уже в `.gitignore`)
2. **Резервное копирование**: регулярно делайте копии `db/appeals.db`
3. **Миграция данных**: используйте скрипт миграции для переноса данных из Google Sheets

## 🔄 Миграция с Google Sheets на БД

1. Установите `USE_DATABASE=true` в `.env`
2. Запустите скрипт миграции (будет создан позже)
3. Проверьте данные в БД
4. Запустите бота

---

**Примечание**: При использовании БД Google Sheets для обращений больше не используется, но может использоваться для авторизации и акций.
