# TODO: Технические Улучшения и Долг

## 🔴 Высокий Приоритет (Надежность)
- [ ] **Внедрить "Безопасный старт" (Graceful Degradation).**
  *Текущее поведение:* Бот падает, если OpenAI или Таблицы недоступны при старте.
  *Желаемое поведение:* Бот запускается в "Безопасном режиме" (работают только базовые команды), логирует ошибку и пытается переподключиться в фоне.
- [ ] **Внедрить "Рубильники" (Feature Flags).**
  Добавить в `.env`: `ENABLE_AI=true/false`, `ENABLE_PROMOTIONS=true/false`.
  Позволяет отключать модули без изменения кода.

## 🟡 Средний Приоритет (Оптимизация)
- [ ] **Изоляция Фоновых Задач.**
  Вынести тяжелые задачи (как массовые рассылки) в отдельный процесс/воркер (например, используя `Celery` или отдельный скрипт-демон), чтобы они не блокировали основного бота.
- [ ] **Структурированное Логирование.**
  Перейти от простого текста к JSON логам для лучшего анализа (особенно если потом подключать внешние системы мониторинга).

## 🟢 Низкий Приоритет (Уборка)
- [ ] **Покрытие Type Hinting.**
  Обеспечить 100% покрытие типов через `mypy` для стабильности кода.
- [ ] **Юнит-тесты.**
  Написать тесты для логики `AuthService`, чтобы избежать регрессии при обновлениях.
