# 📚 Документация алгоритмов: Анализатор недвижимости

> **Версия:** 3.0.9 (Исправлено: Анализ по коду)
> **Файл:** `аналитика и прогноз`
> **Регион:** Тюмень

---

## Что нового в 3.0.9
- **Единый алгоритм DOM:** Теперь и массовый анализ, и анализ по коду используют модель динамической деградации ранга (`calculateDynamicTimeOnMarket`).
- **Исправление ИИ-блока:** Оптимизирована отрисовка экспертного заключения (строка 45+).

---

## ⏱️ Прогнозирование сроков (DOM)

### `calculateTimeForecasts(...)` — Для одиночного объекта
Рассчитывает сроки на основе динамической деградации, сезонности и характеристик объекта.

**Входные параметры:**
- `forecasts`: объект с позициями (quick, market)
- `demandPerMonth`: спрос
- `arrival`: приток новых конкурентов
- `price`: цена объекта
- `activePrices`: массив цен конкурентов

**Алгоритм:**
1. Вычисляет `marketRank` объекта.
2. Вызывает `calculateDynamicTimeOnMarket` для получения базового срока с учётом конкуренции.
3. Применяет сезонный коэффициент (`seasonalCoef`).
4. Применяет фактор комнатности (`roomsFactor`).

---

## 📈 Группы функций

### 1. Анализ по коду (Лист "аналитика ОН по коду")
- `analyzeSingleObjectByCode(code)` — главная функция запуска.
- `calculateSingleObjectMetrics(...)` — расчёт всех показателей.
- `calculateSingleObjectForecasts(...)` — генерация ценовых и временных прогнозов.
- `generateSingleObjectReport(...)` — отрисовка отчёта и ИИ-блока.

### 2. Динамическая деградация
- `calculateDynamicTimeOnMarket(params)` — ядро расчёта сроков.
- `calculatePriceBelowRatio(price, activePrices, count)` — вероятность появления более дешёвого конкурента.

---

## 🤖 Генерация отчётов

### Структура отчёта на листе
| Строки | Раздел |
|--------|--------|
| 4 - 11 | Характеристики вашего объекта |
| 12 - 18| Состояние рынка и ликвидность |
| 19 - 28| Конкурентная позиция и сравнение цен |
| 29 - 33| Рекомендуемые ценовые стратегии |
| 34 - 37| Прогноз по срокам (DOM) |
| 38 - 43| Макроэкономический фон |
| 45+    | **Профессиональное резюме ассистента Gemini** |

---

## Примеры использования

### Расчёт DOM для одиночного объекта
```javascript
// Теперь учитывает приток новых объектов!
const timeForecasts = calculateTimeForecasts(
  forecasts,
  demandPerMonth,
  marketSituation,
  activeCount,
  soldCount,
  objectData,
  arrivalCount, // Новое
  price,        // Новое
  activePrices  // Новое
);
```

---
Документация актуализирована 04.02.2026.
