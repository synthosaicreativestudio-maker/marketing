# ДОКУМЕНТАЦИЯ ФУНКЦИЙ: УНИВЕРСАЛЬНЫЙ АНАЛИЗАТОР НЕДВИЖИМОСТИ

## ОГЛАВЛЕНИЕ

1. [Функции управления и инициализации](#функции-управления-и-инициализации)
2. [Функции подготовки данных](#функции-подготовки-данных)
3. [Функции анализа конкурентов](#функции-анализа-конкурентов)
4. [Функции расчета метрик](#функции-расчета-метрик)
5. [Функции ИИ интеграции](#функции-ии-интеграции)
6. [Функции индивидуального анализа](#функции-индивидуального-анализа)
7. [Функции прогнозирования](#функции-прогнозирования)
8. [Вспомогательные функции](#вспомогательные-функции)
9. [Функции форматирования и отображения](#функции-форматирования-и-отображения)

---

## ФУНКЦИИ УПРАВЛЕНИЯ И ИНИЦИАЛИЗАЦИИ

### `onOpen()`
**Назначение:** Создает пользовательское меню в Google Таблиц при открытии документа.

**Как работает:**
- Вызывается автоматически при открытии таблицы
- Создает меню "Аналитика и прогноз" с пунктами:
  - ▶️ Запустить все функции
  - 🎨 Форматировать листы
  - 🔧 Настроить новые листы
  - 📋 Создать лист критериев
  - 🔑 Установить ключи Gemini
  - 📊 Проверить статус ключей
  - ⚙️ Установить/Переустановить триггеры

**Источники данных:** Не использует внешние данные

**Критерии:** Нет

---

### `installTriggers()`
**Назначение:** Устанавливает автоматические триггеры для запуска скриптов.

**Как работает:**
1. Удаляет все существующие триггеры
2. Создает временные триггеры на 6:00 и 14:00 ежедневно
3. Создает триггер на событие редактирования ячейки B2
4. Показывает уведомление об успешной установке

**Источники данных:** Не использует внешние данные

**Критерии:** 
- Триггеры создаются в часовом поясе 'Asia/Yekaterinburg'
- Временные триггеры запускают `startAllFunctions`
- Триггер редактирования запускает `onEditTrigger`

---

### `startAllFunctions()`
**Назначение:** Основная функция, запускающая весь процесс анализа (оставлена для совместимости).

**Как работает:**
1. Очищает рабочие листы (`clearSheets`)
2. Копирует данные из активной группы (`copyFromActiveGroup`)
3. Копирует данные с Авито в столбцы P-Q (`copyFromSource`)
4. Копирует данные с Циан в столбцы R-S (`copyFromSource`)
5. Копирует данные с Домклик в столбцы T-U (`copyFromSource`)
6. Устанавливает дополнительные заголовки (`setAdditionalHeaders`)
7. Запускает анализ конкурентов (`findCompetitorsForAllObjects`)

**Источники данных:**
- Лист "4. 2. активные в группе"
- Лист "3. 56. авито"
- Лист "5. 56. циан"
- Лист "6. 56. домклик"

**Критерии:** Нет

---

## ФУНКЦИИ ПОДГОТОВКИ ДАННЫХ

### `clearSheets()`
**Назначение:** Очищает рабочие листы от старых данных.

**Как работает:**
- Очищает лист "Аналитика всех ОН в группе" (столбцы A:U)
- Очищает лист "Конкуренты активные и проданные" (столбцы A:O)

**Источники данных:** Не использует внешние данные

**Критерии:** Нет

---

### `copyFromActiveGroup()`
**Назначение:** Копирует данные из листа "4. 2. активные в группе" в основной лист аналитики.

**Как работает:**
1. Получает данные из листа "4. 2. активные в группе" (столбцы A:O)
2. Копирует их в лист "Аналитика всех ОН в группе" (столбцы A:O)
3. Логирует количество скопированных строк

**Источники данных:**
- Лист "4. 2. активные в группе" (столбцы A:O)

**Критерии:** Нет

---

### `copyFromSource(sourceSheetName, startColumn, headers)`
**Назначение:** Копирует данные с указанного листа в основной лист аналитики.

**Параметры:**
- `sourceSheetName` - название исходного листа
- `startColumn` - начальная колонка для вставки
- `headers` - массив заголовков

**Как работает:**
1. Получает данные из исходного листа
2. Находит объекты по коду в основном листе
3. Копирует соответствующие данные в указанные столбцы
4. Устанавливает заголовки

**Источники данных:**
- Указанный лист (Авито, Циан, Домклик)
- Лист "Аналитика всех ОН в группе"

**Критерии:**
- Сопоставление по коду объекта
- Данные копируются только для найденных объектов

---

### `setAdditionalHeaders()`
**Назначение:** Устанавливает дополнительные заголовки для аналитических столбцов.

**Как работает:**
1. Определяет последний столбец с данными
2. Устанавливает заголовки для аналитических столбцов:
   - V: "конкурентов в р-не"
   - W: "продажи всего"
   - X: "спрос, объектов/мес"
   - Y: "Тип ликвидности"
   - Z: "Оценка рынка"
   - AA: "Тренд конкуренции, %"
   - AB: "место/ранг нашего объекта"
   - AC: "Тренд цены за м², руб/мес"
   - AD: "Соотношение с рынком активных, %"
   - AE: "Соотношение с рынком проданных, %"
   - AF: "ср. цена активных"
   - AG: "цена нашего ОН"
   - AH: "разница с активными, %"
   - AI: "ср. цена проданных"
   - AJ: "цена нашего ОН"
   - AK: "разница с проданных, %"
   - AL: "Цена (быстрая продажа)"
   - AM: "Срок (быстрая), мес."
   - AN: "Цена (рыночная)"
   - AO: "Срок (рыночная), мес."
   - AP: (не используется)
   - AQ: (не используется)

**Источники данных:** Не использует внешние данные

**Критерии:** Нет

---

## ФУНКЦИИ АНАЛИЗА КОНКУРЕНТОВ

### `findCompetitorsForAllObjects()`
**Назначение:** Основная функция анализа конкурентов для всех объектов.

**Как работает:**
1. Получает данные из листа "Аналитика всех ОН в группе"
2. Группирует объекты по районам (`groupDataByDistrict`)
3. Для каждого района обрабатывает данные (`processDistrictData`)
4. Форматирует и записывает результаты

**Источники данных:**
- Лист "Аналитика всех ОН в группе"
- Лист "2. 2. активные"
- Лист "1. 2. проданные"

**Критерии:**
- Группировка по районам
- Фильтрация по временным рамкам
- Сопоставление по характеристикам объектов

---

### `groupDataByDistrict(data)`
**Назначение:** Группирует объекты по районам для анализа.

**Параметры:**
- `data` - массив данных объектов

**Как работает:**
1. Создает объект для группировки по районам
2. Для каждого объекта добавляет его в соответствующую группу района
3. Возвращает сгруппированные данные

**Источники данных:** Входной массив данных

**Критерии:**
- Группировка по столбцу 1 (район)

---

### `processDistrictData(params)`
**Назначение:** Обрабатывает данные для конкретного района.

**Параметры:**
- `params` - объект с параметрами района и данными

**Как работает:**
1. Получает активные и проданные объекты
2. Фильтрует конкурентов по критериям
3. Рассчитывает метрики для каждого объекта
4. Форматирует результаты

**Источники данных:**
- Лист "2. 2. активные"
- Лист "1. 2. проданные"

**Критерии:**
- Фильтрация по району
- Фильтрация по типу объекта
- Фильтрация по количеству комнат (±1)
- Фильтрация по площади (±20%)
- Фильтрация по году постройки (±10 лет)
- Фильтрация по типу ремонта (асимметричная логика)

---

### `filterCompetitorsByCriteria(data, criteria, isActiveData = true)`
**Назначение:** Фильтрует конкурентов по заданным критериям.

**Параметры:**
- `data` - массив данных конкурентов
- `criteria` - объект с критериями фильтрации
- `isActiveData` - флаг для активных/проданных данных

**Как работает:**
1. Применяет географический фильтр (район)
2. Применяет фильтр по типу объекта
3. Применяет фильтр по количеству комнат (±1)
4. Применяет фильтр по площади (±20%)
5. Применяет фильтр по году постройки (±10 лет)
6. Применяет асимметричный фильтр по типу ремонта
7. Применяет временной фильтр (6 месяцев для активных, диапазон для проданных)

**Источники данных:** Входной массив данных

**Критерии:**
- Строгое соответствие района
- Строгое соответствие типа объекта
- Допуск ±1 комната
- Допуск ±20% по площади
- Допуск ±10 лет по году постройки
- Асимметричная логика по типу ремонта
- Временные рамки: 6 месяцев для активных, диапазон для проданных

---

## ФУНКЦИИ РАСЧЕТА МЕТРИК

### `calculateObjectMetrics(params)`
**Назначение:** Рассчитывает основные метрики для объекта.

**Параметры:**
- `params` - объект с параметрами для расчета

**Как работает:**
1. Рассчитывает количество конкурентов
2. Рассчитывает количество продаж
3. Рассчитывает спрос (продажи/месяцы)
4. Определяет тип ликвидности
5. Оценивает рынок
6. Рассчитывает тренд конкуренции
7. Рассчитывает ранг объекта
8. Рассчитывает тренд цены
9. Рассчитывает соотношения с рынком
10. Рассчитывает прогнозы

**Источники данных:**
- Активные конкуренты
- Проданные конкуренты
- Временные рамки

**Критерии:**
- Использует усеченное среднее для цен
- Использует медиану для цен
- Линейная регрессия для трендов

---

### `calculateTimeOnMarket(params)`
**Назначение:** Рассчитывает время на рынке для объекта.

**Параметры:**
- `params` - объект с параметрами

**Как работает:**
1. Получает дату вывода в активные
2. Рассчитывает количество дней на рынке
3. Возвращает результат в днях

**Источники данных:**
- Дата вывода в активные (столбец A)

**Критерии:**
- Расчет от даты вывода до текущей даты

---

### `calculatePriceFork(params)`
**Назначение:** Рассчитывает ценовую вилку для объекта.

**Параметры:**
- `params` - объект с параметрами

**Как работает:**
1. Получает цены конкурентов
2. Рассчитывает минимальную и максимальную цены
3. Рассчитывает среднюю цену
4. Возвращает ценовую вилку

**Источники данных:**
- Цены конкурентов (столбец 7)

**Критерии:**
- Использует усеченное среднее
- Исключает экстремальные значения

---

### `calculateQualityScore(objectData)`
**Назначение:** Вычисляет коэффициент качества объекта на основе калиброванных коэффициентов (база — 13,490 сделок).

**Параметры:**
- `objectData` - данные объекта (массив или объект)

**Как работает:**
1. **Ремонт:** Анализирует тип ремонта (Дизайнерский: +0.2, Евро: +0.15, Современный: +0.1, Требует ремонта: -0.15, "Убитая": -0.25).
2. **Год постройки:** Рассчитывает возраст здания (Новостройка до 5 лет: +0.15, Современный 5–15 лет: +0.1, Старый: до -0.1).
3. **Этаж:** Учитывает этажность (Первый: -0.1, Последний: -0.05, Средние этажи: +0.05).
4. **Маркетинг:** Добавляет баллы за профессиональные фото (+0.1), описание >400 символов (+0.1) и наличие планировки (+0.1).

**Источники данных:** Данные объекта (столбцы M, N, O и характеристики ремонта/года).

**Критерии:**
- Коэффициент качества влияет на расчет рекомендуемой цены и сроки продажи.
- Диапазон итогового коэффициента обычно от 0.6 до 1.4.

---

### `getMarketAssessment(liquidityIndex, avgSalesPerMonth)`
**Назначение:** Оценивает состояние рынка.

**Параметры:**
- `liquidityIndex` - индекс ликвидности
- `avgSalesPerMonth` - средние продажи в месяц

**Как работает:**
1. Анализирует индекс ликвидности
2. Анализирует объем продаж
3. Возвращает оценку рынка

**Источники данных:** Входные параметры

**Критерии:**
- Высокий спрос: индекс >20% и продажи >2
- Средний спрос: индекс 10-20% или продажи 1-2
- Низкий спрос: индекс <10% и продажи <1

---

### `determineLiquidityType(competitors, sales, demand)`
**Назначение:** Определяет тип ликвидности (грейд A+/A/B/C/C-).

**Как работает:**
1. Рассчитывает индекс ликвидности: `(demand / competitors) * 100`
2. Возвращает тип по диапазонам

**Применение:** используется в отчёте как краткий индикатор состояния рынка.

---

### `calculateCompetitionTrend(candidates, startDate, endDate)`
**Назначение:** Рассчитывает тренд конкуренции.

**Параметры:**
- `candidates` - массив кандидатов
- `startDate` - начальная дата
- `endDate` - конечная дата

**Как работает:**
1. Разделяет период на две половины
2. Считает конкурентов в каждой половине
3. Рассчитывает процентное изменение
4. Возвращает тренд

**Источники данных:**
- Массив кандидатов
- Временные рамки

**Критерии:**
- Разделение периода пополам
- Подсчет конкурентов в каждой половине

---

## ФУНКЦИИ ИИ ИНТЕГРАЦИИ

### `callGeminiAnalyst(prompt, model)`
**Назначение:** Высокоуровневый вызов ИИ-аналитика для генерации стратегических отчетов.

**Параметры:**
- `prompt` - сформированный запрос с данными
- `model` - предпочтительная модель (gemini-2.0-flash-lite-preview-02-05 или gemini-3-pro)

**Как работает:**
1. Ротирует API-ключи из пула для обхода лимитов.
2. Делегирует выполнение функции `executeGeminiRequest`.
3. Поддерживает автоматический фоллбэк на альтернативные модели.

---

### `getReportSummaryFromSheet(sheet)`
**Назначение:** Собирает аналитическую сводку с листа для передачи в ИИ.

**Как работает:**
1. Считывает диапазон A4:B100.
2. Останавливается ПЕРЕД блоком "🤖 ИТОГОВЫЙ ВЫВОД АНАЛИТИКА", чтобы избежать самоповтора ИИ.
3. Формирует чистый текст для промпта.

---

### `executeGeminiRequest(prompt, model, apiKey)`
**Назначение:** Низкоуровневое выполнение HTTP-запроса к Google Gemini API.

**Как работает:**
1. Устанавливает **Системный Промпт** (роль Эксперта-Стратега, структура PAS).
2. Настраивает параметры генерации (temperature = 0.7).
3. Обрабатывает ошибки 429 (Too Many Requests) и 500.

---

### `showApiStatus()`
**Назначение:** Показывает статус ключей Gemini.

**Как работает:**
1. Вызывает `checkApiKeys`
2. Показывает результат в диалоговом окне

**Источники данных:** Gemini API

**Критерии:** Нет

---

### `checkApiKeys()`
**Назначение:** Проверяет статус ключей Gemini и наличие квот.

**Как работает:**
1. Получает API ключ
2. Отправляет тестовый запрос
3. Анализирует ответ
4. Возвращает статус

**Источники данных:** Gemini API

**Критерии:**
- Проверка доступности API
- Проверка квот и лимитов

---

### `setApiKey()`
**Назначение:** Позволяет пользователю установить ключи Gemini.

**Как работает:**
1. Показывает диалог ввода
2. Сохраняет ключ в PropertiesService
3. Показывает подтверждение

**Источники данных:** Пользовательский ввод

**Критерии:** Нет

---

### `setDefaultApiKey()`
**Назначение:** Устанавливает ключи Gemini по умолчанию.

**Как работает:**
1. Устанавливает предустановленный ключ
2. Сохраняет в PropertiesService
3. Показывает подтверждение

**Источники данных:** Предустановленный ключ

**Критерии:** Нет

---

### `getGeminiApiKey()`
**Назначение:** Получает активный ключ Gemini из пула.

**Как работает:**
1. Читает ключ из PropertiesService
2. Возвращает ключ или null

**Источники данных:** PropertiesService

**Критерии:** Нет

---

## ФУНКЦИИ ИНДИВИДУАЛЬНОГО АНАЛИЗА

### `findObjectByCode(code)`
**Назначение:** Находит объект по коду в листе активных.

**Параметры:**
- `code` - код объекта

**Как работает:**
1. Получает данные из листа "2. 2. активные"
2. Ищет объект с указанным кодом
3. Возвращает данные объекта или null

**Источники данных:**
- Лист "2. 2. активные"

**Критерии:**
- Строгое соответствие кода объекта

---

### Блок "Реклама и охват" (в отчёте)
**Назначение:** справочно показывает статусы/даты рекламы по площадкам и (если есть) просмотры.

**Источник данных:**
- Листы `3. 56. авито`, `5. 56. циан`, `6. 56. домклик`
- Лист `7. 92. просмотры на площадках` (если есть связь по коду)

**Где отображается:** лист "аналитика ОН по коду", перед блоком ценовых стратегий.

---

### `validateCode(code)`
**Назначение:** Проверяет валидность кода объекта.

**Параметры:**
- `code` - код для проверки

**Как работает:**
1. Проверяет, что код не пустой
2. Проверяет, что код - число
3. Возвращает true/false

**Источники данных:** Входной параметр

**Критерии:**
- Код должен быть непустым числом

---

### `buildSingleObjectCompetitorsArray(objectData)`
**Назначение:** Строит массив конкурентов для одного объекта.

**Параметры:**
- `objectData` - данные объекта

**Как работает:**
1. Получает активных и проданных конкурентов
2. Фильтрует их по критериям
3. Форматирует данные
4. Заполняет лист конкурентов
5. Возвращает данные конкурентов

**Источники данных:**
- Лист "2. 2. активные"
- Лист "1. 2. проданные"

**Критерии:**
- Те же критерии фильтрации, что и в `filterCompetitorsByCriteria`

---

### `analyzeSingleObject(objectData, competitorsData)`
**Назначение:** Анализирует один объект.

**Параметры:**
- `objectData` - данные объекта
- `competitorsData` - данные конкурентов

**Как работает:**
1. Получает данные рекламы
2. Рассчитывает метрики
3. Заполняет лист анализа
4. Возвращает результаты

**Источники данных:**
- Данные объекта
- Данные конкурентов
- Данные рекламы

**Критерии:**
- Использует те же критерии расчета метрик

---

### `generateSingleObjectReport(objectData, analysisResults, competitorsData)`
**Назначение:** Генерирует отчет по одному объекту.

**Параметры:**
- `objectData` - данные объекта
- `analysisResults` - результаты анализа
- `competitorsData` - данные конкурентов

**Как работает:**
1. Очищает область отчета
2. Создает структурированные данные
3. Записывает данные построчно
4. Добавляет ИИ анализ
5. Объединяет ячейки для отчета ИИ

**Источники данных:**
- Данные объекта
- Результаты анализа
- Данные конкурентов
- Gemini API

**Критерии:**
- Форматирование заголовков
- Объединение ячеек для ИИ отчета

---

## ФУНКЦИИ ПРОГНОЗИРОВАНИЯ

### `calculateSingleObjectForecasts(objectPrice, objectPricePerM2, marketRatios, demandPerMonth, objectData, activeCompetitors, soldCompetitors)`
**Назначение:** Рассчитывает прогнозы для одного объекта.

**Параметры:**
- `objectPrice` - цена объекта
- `objectPricePerM2` - цена за м²
- `marketRatios` - соотношения с рынком
- `demandPerMonth` - спрос в месяц
- `objectData` - данные объекта
- `activeCompetitors` - активные конкуренты
- `soldCompetitors` - проданные конкуренты

**Как работает:**
1. Рассчитывает коэффициент качества
2. Оценивает ситуацию на рынке
3. Рассчитывает альтернативную цену продажи
4. Рассчитывает прогнозы по позиции
5. Рассчитывает временные прогнозы

**Источники данных:**
- Данные объекта
- Данные конкурентов
- Рыночные соотношения

**Критерии:**
- Использует коэффициент качества
- Учитывает ситуацию на рынке
- Рассчитывает стратегии: быстрая и рыночная
- Рассчитывает сроки: при быстрой, рыночной и текущей цене
- Позиционирование и сравнение — по цене за м²; рекомендации цены — по полной цене

---

### `calculateForecastsByPosition(objectPrice, baseSoldPrice, baseActivePrice, baseMarketPrice, qualityScore, marketSituation, activeCompetitors, soldCompetitors, demandPerMonth)`
**Назначение:** Рассчитывает прогнозы на основе позиции объекта.

**Параметры:**
- `objectPrice` - цена объекта
- `baseSoldPrice` - базовая цена продажи
- `baseActivePrice` - базовая активная цена
- `baseMarketPrice` - базовая рыночная цена
- `qualityScore` - коэффициент качества
- `marketSituation` - ситуация на рынке
- `activeCompetitors` - активные конкуренты
- `soldCompetitors` - проданные конкуренты
- `demandPerMonth` - спрос в месяц

**Как работает:**
1. Определяет базовую цену
2. Рассчитывает быструю цену (около 87% от базовой)
3. Рассчитывает рыночную цену (100% от базовой)
4. Корректирует цены на основе спроса

**Источники данных:**
- Цены объекта и конкурентов
- Коэффициент качества
- Ситуация на рынке

**Критерии:**
- Быстрая цена <= рыночная цена
- Корректировка на основе спроса

---

### `calculateTimeForecasts(forecasts, demandPerMonth, marketSituation, activeCount, soldCount)`
**Назначение:** Рассчитывает временные прогнозы.

**Параметры:**
- `forecasts` - прогнозы цен
- `demandPerMonth` - спрос в месяц
- `marketSituation` - ситуация на рынке
- `activeCount` - количество активных
- `soldCount` - количество проданных

**Как работает:**
1. Рассчитывает время для быстрой продажи
2. Рассчитывает время для рыночной продажи
3. Рассчитывает время для рыночной продажи (динамическая модель)
4. Учитывает позицию объекта и спрос

**Источники данных:**
- Прогнозы цен
- Данные о спросе
- Ситуация на рынке

**Критерии:**
- Минимум 1 месяц для всех прогнозов
- Учет позиции объекта

---

## ВСПОМОГАТЕЛЬНЫЕ ФУНКЦИИ

### `cleanDataValue(value)`
**Назначение:** Очищает значение от недопустимых данных.

**Параметры:**
- `value` - значение для очистки

**Как работает:**
1. Проверяет на null, undefined, 0, 9
2. Возвращает очищенное значение или null

**Источники данных:** Входное значение

**Критерии:**
- Исключает null, undefined, 0, 9

---

### `calculateTrimmedMean(prices)`
**Назначение:** Рассчитывает усеченное среднее для цен.

**Параметры:**
- `prices` - массив цен

**Как работает:**
1. Сортирует цены
2. Удаляет 20% экстремальных значений
3. Рассчитывает среднее оставшихся значений

**Источники данных:** Массив цен

**Критерии:**
- Удаление 20% экстремальных значений
- Использование оставшихся 80% значений

---

### `calculateMedian(prices)`
**Назначение:** Рассчитывает медиану для цен.

**Параметры:**
- `prices` - массив цен

**Как работает:**
1. Сортирует цены
2. Находит средний элемент
3. Возвращает медиану

**Источники данных:** Массив цен

**Критерии:**
- Сортировка по возрастанию
- Выбор среднего элемента

---

### `calculateLinearRegression(points)`
**Назначение:** Рассчитывает линейную регрессию для трендов.

**Параметры:**
- `points` - массив точек данных

**Как работает:**
1. Рассчитывает коэффициенты регрессии
2. Возвращает наклон и пересечение

**Источники данных:** Массив точек

**Критерии:**
- Метод наименьших квадратов

---

## ФУНКЦИИ ФОРМАТИРОВАНИЯ И ОТОБРАЖЕНИЯ

### `formatCompetitorRow(row, isSold)`
**Назначение:** Форматирует строку конкурента для отображения.

**Параметры:**
- `row` - строка данных
- `isSold` - флаг проданного объекта

**Как работает:**
1. Форматирует данные объекта
2. Добавляет метку "ПРОДАН" для проданных
3. Возвращает отформатированную строку

**Источники данных:** Строка данных

**Критерии:**
- Форматирование дат
- Добавление меток

---

### `fillCompetitorsSheet(activeCompetitors, soldCompetitors, objectCode, objectData)`
**Назначение:** Заполняет лист конкурентов.

**Параметры:**
- `activeCompetitors` - активные конкуренты
- `soldCompetitors` - проданные конкуренты
- `objectCode` - код объекта
- `objectData` - данные объекта

**Как работает:**
1. Очищает лист.
2. Добавляет заголовки.
3. Добавляет активных конкурентов.
4. Добавляет проданных конкурентов.
5. Сортирует по цене.
6. Форматирует данные.

---

### `formatReportSheet(sheetName)`
**Назначение:** Форматирует лист отчета для профессионального отображения.

**Как работает:**
1. Находит диапазон данных отчета.
2. Устанавливает ширину колонок и шрифты (Roboto/Inter).
3. Применяет цветовое кодирование для заголовков.
4. Выравнивает текст и включает автоматический перенос.
5. Вызывает `autoResizeRows` для динамического подбора высоты строк.

---

### `createSingleObjectReportData(objectData, analysisResults, competitorsData)`
**Назначение:** Создает структурированные данные отчета.

**Параметры:**
- `objectData` - данные объекта
- `analysisResults` - результаты анализа
- `competitorsData` - данные конкурентов

**Как работает:**
1. Создает массив данных отчета
2. Форматирует числовые значения
3. Добавляет описательные тексты
4. Возвращает структурированные данные

**Источники данных:**
- Данные объекта
- Результаты анализа
- Данные конкурентов

**Критерии:**
- Форматирование чисел
- Добавление описательных текстов

---

### `createSingleObjectReportText(objectData, analysisResults, competitorsData)`
**Назначение:** Создает текстовый отчет с помощью ИИ.

**Параметры:**
- `objectData` - данные объекта
- `analysisResults` - результаты анализа
- `competitorsData` - данные конкурентов

**Как работает:**
1. Создает промпт для ИИ
2. Вызывает Gemini API
3. Возвращает сгенерированный текст

**Источники данных:**
- Данные объекта
- Результаты анализа
- Данные конкурентов
- Gemini API

**Критерии:**
- Профессиональный стиль
- Сохранение всех данных
- Структурированный отчет

---

## ЗАКЛЮЧЕНИЕ

Данная документация описывает более 80 функций системы анализа недвижимости. Каждая функция имеет четкое назначение, использует определенные источники данных и применяет конкретные критерии для обработки информации.

Система построена по модульному принципу, где каждая функция отвечает за свою область ответственности, что обеспечивает гибкость, надежность и возможность легкого расширения функциональности.

Основные принципы работы системы:
- Строгая фильтрация конкурентов по множественным критериям
- Использование статистических методов (усеченное среднее, медиана, линейная регрессия)
- Интеграция с ИИ для генерации профессиональных отчетов
- Автоматизация всех процессов анализа
- Детальное логирование для отладки и мониторинга
